
   localStorage.setItem()
    localStorage.getItem()


    JSON ：
    JSON.parse('[1,2,3]') = [1,2,3]

    JSON.stringify([1,2,3]) = '[1,2,3]'


给元素加类名
    div.dataset.xx = ?

数组：
    往数组后面
    
    push()      添加
    pop()       删除

    往数组前面
    unshift()   添加
    shift（)    删除

    concat()    连接数组